AJUSTE V5 - ENTRADAS DO DASHBOARD + FUSO HORÁRIO

1. ENTRADAS / DASHBOARD
- Corrigido o cadastro de nova entrada para gravar HistoricoMovimentacao.quantidade.
- O gráfico Entradas x Saídas já soma o campo quantidade, então novas entradas passam a subir corretamente.
- Incluída a migration 0041 para reparar entradas antigas que ficaram com quantidade 0 por esse bug.
- A migration só altera históricos do tipo exato "Entrada", quantidade=0 e cuja descrição contenha "Entrada de X unidades".

2. HORA CORRETA
- settings.py já permanece com TIME_ZONE='America/Sao_Paulo' e USE_TZ=True.
- dockerfile agora define TZ=America/Sao_Paulo, instala tzdata e configura /etc/localtime.
- Isso corrige `date`, cron e logs do container.
- Formatações manuais em Python foram ajustadas para timezone.localtime antes de strftime nos pontos encontrados do sapp.
- O banco continua armazenando timestamps de forma segura com USE_TZ=True.

3. DEPLOY
- Esta versão TEM migration nova: 0041_corrigir_quantidade_historico_entrada.py
- Depois do deploy: python manage.py migrate
- Para conferir o horário do container: date
- Para conferir o Django: python manage.py shell -c "from django.utils import timezone; print(timezone.localtime())"
